import { DisplayColumnPipe } from './display-column.pipe';

describe('DisplayColumnPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayColumnPipe();
    expect(pipe).toBeTruthy();
  });
});
