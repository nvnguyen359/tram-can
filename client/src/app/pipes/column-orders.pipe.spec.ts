import { ColumnOrdersPipe } from './column-orders.pipe';

describe('ColumnOrdersPipe', () => {
  it('create an instance', () => {
    const pipe = new ColumnOrdersPipe();
    expect(pipe).toBeTruthy();
  });
});
