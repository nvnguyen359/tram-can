import { TextAlignNumberPipe } from './text-align-number.pipe';

describe('TextAlignNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new TextAlignNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
